# Code commented out as github actions workflow deploy code that crashes website

# # This file was auto-generated by the Firebase CLI
# # https://github.com/firebase/firebase-tools

# name: Deploy to Firebase Hosting on PR
# "on": pull_request
# jobs:
#   build_and_preview:
#     if: "${{ github.event.pull_request.head.repo.full_name == github.repository }}"
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v2
#       - run: npm ci && npm run build
#       - uses: FirebaseExtended/action-hosting-deploy@v0
#         with:
#           # repoToken: '${{ secrets.GITHUB_TOKEN }}' //original
#           repoToken: "${{ secrets.FIREBASE_TOKEN }}"
#           # //attempt after adding FIREBASE_TOKEN to github secrets
#           firebaseServiceAccount: "${{ secrets.FIREBASE_SERVICE_ACCOUNT_BAKESWITCH_ORBITAL }}"
#           projectId: bakeswitch-orbital
